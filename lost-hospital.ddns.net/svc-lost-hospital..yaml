apiVersion: v1
kind: Service
metadata:
  name: svc-lost-hospital
  namespace: lost-hospital
spec:
  type: NodePort
  selector:
    app: nginx-lost-hospital
  ports:
    - port: 8080
      targetPort: 5000