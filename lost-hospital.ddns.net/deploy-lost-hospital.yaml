apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-lost-hospital
  namespace: lost-hospital
spec:
  selector:
    matchLabels:
      app: deploy-lost-hospital
  replicas: 1
  template:
    metadata:
      labels:
        app: deploy-lost-hospital
    spec:
      containers:
      - name: deploy-lost-hospital
        image: puurl/alp-lost-hospital:v2
        ports:
        - containerPort: 3000
